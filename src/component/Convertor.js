export const Convertor = ({code}) => {
    return (
        <div>
            <p>
                <label htmlFor="">from {code}</label>
            </p>
            <p>
                <input type="text"/>
            </p>
        </div>
    );
}